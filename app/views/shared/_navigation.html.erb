<nav class="bg-white shadow-sm sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <div class="flex items-center">
        <%= link_to root_path, class: "text-2xl font-bold text-amber-600" do %>
          ☕ CoffeeShop
        <% end %>
      </div>
      <% if local_assigns[:show_menu] != false %>
        <div class="hidden md:flex space-x-8">
          <%= link_to "Home", root_path, class: local_assigns[:active] == :home ? "text-gray-900 font-semibold border-b-2 border-amber-600" : "text-gray-600 hover:text-gray-900" %>
          <%= link_to "Coffees", coffees_path, class: local_assigns[:active] == :coffees ? "text-gray-900 font-semibold border-b-2 border-amber-600" : "text-gray-600 hover:text-gray-900" %>
          <% if Current.user %>
            <%= link_to "Cart (#{Current.user.current_cart.order_items.sum(:quantity)})", cart_path, class: local_assigns[:active] == :cart ? "text-gray-900 font-semibold border-b-2 border-amber-600" : "text-gray-600 hover:text-gray-900" %>
            <%= link_to "Orders", orders_path, class: local_assigns[:active] == :orders ? "text-gray-900 font-semibold border-b-2 border-amber-600" : "text-gray-600 hover:text-gray-900" %>
          <% else %>
            <%= link_to "Cart (0)", cart_path, class: "text-gray-600 hover:text-gray-900" %>
          <% end %>
        </div>
      <% end %>
      <div class="flex items-center space-x-4">
        <% if Current.user %>
          <span class="text-gray-600"><%= Current.user.email_address %></span>
          <%= button_to "Logout", session_path, method: :delete, class: "text-gray-600 hover:text-gray-900" %>
        <% else %>
          <%= link_to "Login", new_session_path, class: "text-gray-600 hover:text-gray-900" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
